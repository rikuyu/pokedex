name: pokedex
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  auto_route: ^7.7.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  convert: ^3.1.1
  dio: ^5.3.0
  hooks_riverpod: ^2.3.6
  logger: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  auto_route_generator: ^7.2.0
  freezed: ^2.4.1
  json_serializable: ^6.7.1
  build_runner: ^2.4.6

flutter:
  assets:
    - assets/json/pokemon_name.json
  uses-material-design: true
